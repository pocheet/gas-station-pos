# render.yaml
services:
  - type: web
    name: gas-station-pos
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18